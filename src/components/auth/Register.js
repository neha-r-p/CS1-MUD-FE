import React from 'react';
import AuthForm from './AuthForm';

function Register() {
  return (
    <div>
        <AuthForm isLogin={false} />
        Register
    </div>
  );
}

export default Register;
